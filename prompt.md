You have access to the file I uploaded: “assignment.txt” in knowledge history.

Please first analyze this PDF in depth and extract every required functionality, technical requirement, and expected deliverable. Understand it completely, including all sections such as subscription management, webhook ingestion, asynchronous processing, retry strategies, delivery logging, data retention, analytics, minimal UI, containerization, and deployment. Consider everything mentioned in the PDF as mandatory — there are no bonus or optional elements.

Now, based on that full understanding, help me generate a complete production-grade backend project using the following stack:
	•	Python with FastAPI for the API layer
	•	PostgreSQL for persistent data (subscriptions, delivery logs)
	•	Redis for caching and Celery broker
	•	Celery for background delivery processing and scheduled cleanup tasks
	•	Docker Compose to orchestrate all services (app, worker, DB, Redis)
	•	Pytest for testing (unit + integration scaffolds)
	•	Optional minimal UI: Swagger/OpenAPI (auto-generated by FastAPI) or a basic React/HTMX interface

The following features must be implemented in full, cleanly structured, with modular code and clear docstrings:

⸻

1. Subscription Management
	•	Full CRUD endpoints:
	•	POST /subscriptions
	•	GET /subscriptions/{id}
	•	PUT /subscriptions/{id}
	•	DELETE /subscriptions/{id}
	•	Fields:
	•	Unique UUID
	•	Target URL
	•	Optional secret_key (used for HMAC SHA256 signature verification)
	•	Allowed event_types (list of strings)

⸻

2. Webhook Ingestion
	•	Endpoint: POST /ingest/{subscription_id}
	•	Accepts:
	•	Raw JSON body (payload)
	•	Header: X-Hub-Signature-256 containing HMAC-SHA256 signature (optional, based on subscription secret)
	•	Header or param: event_type
	•	Functionality:
	•	Verify signature (if subscription has secret key)
	•	Check if event_type is allowed for this subscription
	•	If valid, enqueue webhook delivery task and return 202 Accepted immediately

⸻

3. Asynchronous Delivery + Retry Logic
	•	Background Celery worker will:
	•	Fetch subscription details (preferably from Redis cache)
	•	POST the payload to the target URL with a timeout (5–10 seconds)
	•	On failure (non-2xx status or timeout), retry with exponential backoff:
	•	Delays: 10s, 30s, 1m, 5m, 15m
	•	Max attempts: 5
	•	Mark as failed if all retries fail

⸻

4. Delivery Logging
	•	Store in PostgreSQL:
	•	Delivery attempt ID (UUID)
	•	Subscription ID
	•	Target URL
	•	Payload
	•	Event type
	•	Timestamp
	•	Attempt number (1 to 5)
	•	HTTP status code
	•	Outcome (Success, Failed, Final Failure)
	•	Error details if any

⸻

5. Log Retention
	•	Run a background scheduled job (Celery Beat or APScheduler) to:
	•	Delete all logs older than 72 hours
	•	Should run periodically (e.g., hourly)

⸻

6. Status & Analytics APIs
	•	GET /status/{delivery_id} – Fetch full delivery history and outcome for a specific webhook
	•	GET /subscriptions/{id}/attempts?limit=20 – Recent delivery attempts for a subscription

⸻

7. Caching
	•	Use Redis to cache subscription metadata (target URL, secret key, allowed event types) keyed by subscription ID
	•	Set sensible TTL (e.g., 5–10 minutes)

⸻

8. Minimal UI for Testing
	•	Must allow testing the following:
	•	Create/update/delete subscriptions
	•	Send test webhook payloads
	•	View delivery logs/status
	•	Swagger/OpenAPI docs should be auto-enabled (and styled cleanly)
	•	 a basic custom UI using ReactJS can be added

⸻

9. Containerization & Deployment
	•	Use Docker Compose to:
	•	Build and run: FastAPI app, PostgreSQL, Redis, Celery worker, optional Flower
	•	Support .env-based configuration (provide .env.example)
	•	Include full Dockerfile, docker-compose.yml, and all configuration
	•	Prepare for deployment to a free-tier cloud provider (consider Render)

⸻

10. Checklist & Commits
	•	Create a checklist.md file that divides the whole project into clear, trackable phases:
	•	Phase 0: Initialization (structure, Docker, dependencies)
	•	Phase 1: Subscription API
	•	Phase 2: Ingestion + Verification + Event Filtering
	•	Phase 3: Async Delivery + Retry
	•	Phase 4: Delivery Logging + Status APIs
	•	Phase 5: Log Retention Job
	•	Phase 6: Minimal UI
	•	Phase 7: Final Polish + Deployment
	•	After each phase, make a clean, descriptive Git commit to track milestones

⸻

11. Other Requirements
	•	Clear modular folder structure: /app, /api, /schemas, /models, /workers, /services, /utils, /tests
	•	Use Pydantic for input/output schemas
	•	Use SQLAlchemy  (async mode) or the databases lib
	•	Add test scaffolding with pytest
	•	Ensure all routes are documented via OpenAPI
	•	README must include:
	•	Setup instructions (Docker)
	•	Architecture explanation
	•	curl examples for every endpoint
	•	Cost estimate for 5K webhooks/day on a free-tier

⸻

Please generate the entire codebase from scratch, as if you are setting up the project for real-world production. The output should include:
	•	Folder structure
	•	All main modules and files (or detailed code blocks per file)
	•	Dockerfiles and compose setup
	•	Configs and .env.example
	•	README and checklist.md scaffolds
	•	Proper docstrings and typing
	•	Placeholder test cases (at least 1 unit + 1 integration)

This project will be used for a shortlisting assessment and should reflect perfect tracking, best practices, and production standards.